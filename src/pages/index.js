import Layout from "./layouts";

function IndexPage() {
    return <Layout><h1>Check this shiz out!  I've got my own components installed and running.  We're using React and Styled Components.  We're also using graphql and apollo now.  Just today 16 May 21 we added routing via nextjs.  This has turned into a cool project!</h1></Layout>
}

export default IndexPage;